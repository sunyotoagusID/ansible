---
- name: Update Debian/Ubuntu Servers
  hosts: debian_servers
  become: true
  tasks:
    - name: Update apt repo and cache
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: Safe upgrade packages (Patch & Bugfix)
      apt:
        upgrade: safe
