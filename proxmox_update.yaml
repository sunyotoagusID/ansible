---
- name: Update Proxmox Servers
  hosts: proxmox_servers
  become: yes
  tasks:
    - name: Update apt repo and cache
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: Upgrade all packages (Proxmox wajib menggunakan dist)
      apt:
        upgrade: dist
